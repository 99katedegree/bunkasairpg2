openapi: 3.0.3
info:
  title: Bunkasai RPG API
  version: 0.1.0
  description: 文化祭RPG API

servers:
  - url: http://localhost:8085
    description: Local

tags:
  - name: health
    description: ヘルスチェック
  - name: auth
    description: 認証
  - name: image
    description: 画像
  - name: me
    description: ログインユーザー
  - name: monster
    description: モンスター
  - name: item
    description: アイテム
  - name: weapon
    description: 武器
  - name: action
    description: アクション
  - name: battle
    description: バトル
  - name: boss
    description: ボスバトル
  - name: game
    description: ゲーム管理

paths:
  /health:
    get:
      tags: [health]
      summary: ヘルスチェック
      operationId: healthCheck
      responses:
        '200':
          description: 正常
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    example: ok

  /images:
    post:
      tags: [image]
      summary: 画像アップロード
      operationId: uploadImage
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [imageFile, directory]
              properties:
                imageFile:
                  type: string
                  format: binary
                directory:
                  type: string
      responses:
        '200':
          description: アップロード成功
          content:
            application/json:
              schema:
                type: object
                required: [image]
                properties:
                  image:
                    $ref: '#/components/schemas/ImageResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/user-login:
    post:
      tags: [auth]
      summary: ユーザーログイン
      operationId: userLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserLoginRequest'
      responses:
        '200':
          description: ログイン成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: 認証エラー
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                errors: [USERLOGIN_USERNOTFOUND]

  /auth/admin-login:
    post:
      tags: [auth]
      summary: 管理者ログイン
      operationId: adminLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminLoginRequest'
      responses:
        '200':
          description: ログイン成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: 認証エラー
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                errors: [ADMINLOGIN_INVALID]

  /me:
    get:
      tags: [me]
      summary: ログインユーザー情報取得
      operationId: getMe
      security:
        - bearerAuth: []
      responses:
        '200':
          description: ユーザー情報
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user:
                    $ref: '#/components/schemas/MeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags: [me]
      summary: ログインユーザー情報更新
      operationId: updateMe
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMeRequest'
      responses:
        '204':
          description: 更新成功
        '401':
          $ref: '#/components/responses/Unauthorized'

  /items:
    get:
      tags: [item]
      summary: 所持アイテム一覧取得
      operationId: getItems
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 所持アイテム一覧
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserItemResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /actions/use-items:
    patch:
      tags: [action]
      summary: アイテム使用
      operationId: useMeItem
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UseItemRequest'
      responses:
        '204':
          description: 使用成功
        '400':
          description: リクエストエラー
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                itemNotFound:
                  summary: アイテムが存在しない
                  value:
                    errors: [USEITEM_ITEMNOTFOUND]
                itemStockEmpty:
                  summary: アイテムの在庫が0
                  value:
                    errors: [USEITEM_ITEMSTOCKEMPTY]
        '401':
          $ref: '#/components/responses/Unauthorized'

  /weapons:
    get:
      tags: [weapon]
      summary: 所持武器一覧取得
      operationId: getWeapons
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 所持武器一覧
          content:
            application/json:
              schema:
                type: object
                required: [weapons]
                properties:
                  weapons:
                    type: array
                    items:
                      $ref: '#/components/schemas/WeaponResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /actions/change-weapons:
    patch:
      tags: [action]
      summary: 装備武器変更
      operationId: changeMeWeapon
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeWeaponRequest'
      responses:
        '204':
          description: 変更成功
        '400':
          description: リクエストエラー
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                errors: [CHANGEWEAPON_WEAPONNOTOWNED]
        '401':
          $ref: '#/components/responses/Unauthorized'

  /battles/start:
    post:
      tags: [battle]
      summary: バトル開始（モンスター図鑑未登録なら自動登録）
      operationId: startBattle
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BattleStartRequest'
      responses:
        '200':
          description: バトル開始成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BattleStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: モンスターが存在しない
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                errors: [BATTLESTART_MONSTERNOTFOUND]

  /battles/finish:
    post:
      tags: [battle]
      summary: バトル終了
      operationId: finishBattle
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BattleFinishRequest'
      responses:
        '200':
          description: バトル勝利・報酬付与
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BattleRewardResponse'
        '400':
          description: バトル敗北または結果不正
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                lost:
                  summary: バトル敗北
                  value:
                    errors: [BATTLEFINISH_LOST]
                invalid:
                  summary: 結果が再現と一致しない
                  value:
                    errors: [BATTLEFINISH_INVALID]
                expired:
                  summary: トークン期限切れ
                  value:
                    errors: [BATTLEFINISH_EXPIRED]
        '401':
          $ref: '#/components/responses/Unauthorized'

  /monsters/index:
    get:
      tags: [monster]
      summary: モンスター図鑑取得
      operationId: getMonsters
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Offset'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: モンスター図鑑
          content:
            application/json:
              schema:
                type: object
                required: [monsters, total]
                properties:
                  monsters:
                    type: array
                    items:
                      nullable: true
                      allOf:
                        - $ref: '#/components/schemas/MonsterCatalogResponse'
                  total:
                    type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'

  /weapons/index:
    get:
      tags: [weapon]
      summary: 武器図鑑取得
      operationId: getMeWeaponIndex
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Offset'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: 武器図鑑
          content:
            application/json:
              schema:
                type: object
                required: [weapons, total]
                properties:
                  weapons:
                    type: array
                    items:
                      nullable: true
                      allOf:
                        - $ref: '#/components/schemas/WeaponResponse'
                  total:
                    type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'

  /items/index:
    get:
      tags: [item]
      summary: アイテム図鑑取得
      operationId: getMeItemIndex
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/Offset'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: アイテム図鑑
          content:
            application/json:
              schema:
                type: object
                required: [items, total]
                properties:
                  items:
                    type: array
                    items:
                      nullable: true
                      allOf:
                        - $ref: '#/components/schemas/ItemResponse'
                  total:
                    type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'

  /boss-battles/start:
    post:
      tags: [boss]
      summary: ボスバトル開始
      operationId: startBossBattle
      security:
        - bearerAuth: []
      responses:
        '200':
          description: ボスバトル開始成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BattleStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /boss-battles/finish:
    post:
      tags: [boss]
      summary: ボスバトル終了（クリアタイムはサーバーが計測）
      operationId: finishBossBattle
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BattleFinishRequest'
      responses:
        '200':
          description: ボス撃破・クリア記録登録
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BossFinishResponse'
        '400':
          description: バトル敗北または結果不正
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                lost:
                  summary: バトル敗北
                  value:
                    errors: [BOSSFINISH_LOST]
                invalid:
                  summary: 結果が再現と一致しない
                  value:
                    errors: [BOSSFINISH_INVALID]
                expired:
                  summary: トークン期限切れ
                  value:
                    errors: [BOSSFINISH_EXPIRED]
        '401':
          $ref: '#/components/responses/Unauthorized'

  /monsters/summaries:
    get:
      tags: [monster]
      summary: "[管理者専用] 全モンスターの一覧取得"
      description: |
        マスタに登録されている全モンスターを図鑑番号の昇順で返す。管理画面が
        「何が存在するか」を把握するために使う。どの画像を割り当てるかは
        フロント側の関心なので、ここでは画像情報は一切返さない。
      operationId: getMonsterSummaries
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 全モンスター一覧
          content:
            application/json:
              schema:
                type: object
                required: [monsters]
                properties:
                  monsters:
                    type: array
                    items:
                      $ref: '#/components/schemas/MonsterSummaryResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /weapons/summaries:
    get:
      tags: [weapon]
      summary: "[管理者専用] 全武器の一覧取得"
      description: |
        マスタに登録されている全武器を図鑑番号の昇順で返す。管理画面が
        「何が存在するか」を把握するために使う。どの画像を割り当てるかは
        フロント側の関心なので、ここでは画像情報は一切返さない。
      operationId: getWeaponSummaries
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 全武器一覧
          content:
            application/json:
              schema:
                type: object
                required: [weapons]
                properties:
                  weapons:
                    type: array
                    items:
                      $ref: '#/components/schemas/WeaponSummaryResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /items/summaries:
    get:
      tags: [item]
      summary: "[管理者専用] 全アイテムの一覧取得"
      description: |
        マスタに登録されている全アイテムを図鑑番号の昇順で返す。管理画面が
        「何が存在するか」を把握するために使う。どの画像を割り当てるかは
        フロント側の関心なので、ここでは画像情報は一切返さない。
      operationId: getItemSummaries
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 全アイテム一覧
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/ItemSummaryResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /monsters/battle-tokens:
    get:
      tags: [monster]
      summary: "[管理者専用] 全モンスターの暗号化バトルトークン一覧取得"
      operationId: getMonsterBattleTokens
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 暗号化済みモンスタートークン一覧
          content:
            application/json:
              schema:
                type: object
                required: [tokens]
                properties:
                  tokens:
                    type: array
                    items:
                      type: string
        '401':
          $ref: '#/components/responses/Unauthorized'

  /game/start:
    post:
      tags: [game]
      summary: "[管理者専用] 新規ゲームスタート"
      description: |
        既存ユーザーをアーカイブし（未ログインユーザーは削除）、指定数の新規ユーザーを作成する。
        作成した全ユーザーのIDを返す（クライアント側でログインQR PDFを生成するために使用）。
      operationId: startGame
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GameStartRequest'
      responses:
        '200':
          description: 新規ユーザーID一覧
          content:
            application/json:
              schema:
                type: object
                required: [userIds]
                properties:
                  userIds:
                    type: array
                    items:
                      type: string
                      format: uuid
        '401':
          $ref: '#/components/responses/Unauthorized'

  /game/archive:
    post:
      tags: [game]
      summary: "[管理者専用] 一括アーカイブ"
      description: 既存ユーザーをアーカイブする。未ログインユーザーのレコードは削除する。
      operationId: archiveGame
      security:
        - bearerAuth: []
      responses:
        '204':
          description: アーカイブ完了
        '401':
          $ref: '#/components/responses/Unauthorized'

  /monsters/{monsterId}:
    get:
      tags: [monster]
      summary: モンスター詳細取得
      operationId: getMonster
      security:
        - bearerAuth: []
      parameters:
        - name: monsterId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: モンスター詳細
          content:
            application/json:
              schema:
                type: object
                required: [monster]
                properties:
                  monster:
                    $ref: '#/components/schemas/MonsterDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: リソースが見つからない
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                errors: [GETMONSTER_NOTFOUND]

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

  parameters:
    Offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        default: 20

  responses:
    Unauthorized:
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            errors: [UNAUTHORIZED]
    BadRequest:
      description: リクエストエラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            errors: [BADREQUEST]
    NotFound:
      description: リソースが見つからない
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            errors: [NOTFOUND]

  schemas:
    PhysicsType:
      type: string
      enum: [slash, blow, shoot]

    ElementType:
      type: string
      enum: [neutral, flame, water, wood, shine, dark]

    EffectType:
      type: string
      enum: [heal, buff, debuff]

    ErrorResponse:
      type: object
      required: [errors]
      properties:
        errors:
          type: array
          items:
            type: string

    ImageResponse:
      type: object
      required: [id, url]
      properties:
        id:
          type: integer
        url:
          type: string

    UserLoginRequest:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid

    AdminLoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    AuthResponse:
      type: object
      required: [authToken]
      properties:
        authToken:
          type: string

    MonsterSummaryResponse:
      description: 管理画面向けのモンスターの要約。
      type: object
      required: [id, name, indexNumber]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        indexNumber:
          description: 図鑑番号（4 桁の数字）
          type: string
        imageUrl:
          description: 画像がまだ用意されていない個体は null
          type: string
          nullable: true

    WeaponSummaryResponse:
      description: 管理画面向けの武器の要約。
      type: object
      required: [id, name, indexNumber]
      properties:
        id:
          type: integer
        name:
          type: string
        indexNumber:
          description: 図鑑番号（4 桁の数字）
          type: string
        imageUrl:
          description: 画像がまだ用意されていない武器は null
          type: string
          nullable: true

    ItemSummaryResponse:
      description: 管理画面向けのアイテムの要約。
      type: object
      required: [id, name, indexNumber]
      properties:
        id:
          type: integer
        name:
          type: string
        indexNumber:
          description: 図鑑番号（4 桁の数字）
          type: string
        imageUrl:
          description: 画像がまだ用意されていないアイテムは null
          type: string
          nullable: true

    WeaponResponse:
      type: object
      required: [id, name, physicsAttack, elementAttack, physicsType, elementType]
      properties:
        id:
          type: integer
        name:
          type: string
        physicsAttack:
          type: number
        elementAttack:
          type: number
          nullable: true
        physicsType:
          $ref: '#/components/schemas/PhysicsType'
        elementType:
          $ref: '#/components/schemas/ElementType'
        imageUrl:
          type: string
          nullable: true

    MeResponse:
      type: object
      required: [id, name, level, hitPoint, experiencePoint]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        avatarImageUrl:
          type: string
          nullable: true
        level:
          type: integer
        hitPoint:
          type: integer
        experiencePoint:
          type: integer
        weapon:
          $ref: '#/components/schemas/WeaponResponse'
          nullable: true

    UpdateMeRequest:
      description: |
        プロフィールに関わる項目のみ変更できる。
        レベル・HP・経験値はバトル終了時にサーバーが決めるため受け付けない。
      type: object
      properties:
        name:
          type: string
        equippedWeaponId:
          type: integer
          description: 所持している武器のみ指定できる
        avatarImageId:
          type: integer

    ItemResponse:
      type: object
      required: [id, name, effectType]
      properties:
        id:
          type: integer
        name:
          type: string
        effectType:
          $ref: '#/components/schemas/EffectType'
        amount:
          type: integer
          description: heal のみ
        rate:
          type: number
          description: buff / debuff のみ
        target:
          type: string
          description: buff / debuff のみ
        imageUrl:
          type: string
          nullable: true

    UserItemResponse:
      allOf:
        - $ref: '#/components/schemas/ItemResponse'
        - type: object
          required: [count]
          properties:
            count:
              type: integer

    MonsterCatalogResponse:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid

    MonsterDetailResponse:
      type: object
      # 耐性9軸は必須。DB のカラムが NOT NULL で「未設定」が存在せず、0.0 は
      # 「等倍」という意味のある値なので、省略可能にすると受け手が 0.0 と
      # 未設定を区別できない。実際それでダメージ計算が壊れていた。
      required:
        [
          id,
          name,
          attack,
          hitPoint,
          experiencePoint,
          recommendedLevel,
          slash,
          blow,
          shoot,
          neutral,
          flame,
          water,
          wood,
          shine,
          dark,
        ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        attack:
          type: integer
        hitPoint:
          type: integer
        experiencePoint:
          type: integer
        recommendedLevel:
          type: integer
        slash:
          type: number
        blow:
          type: number
        shoot:
          type: number
        neutral:
          type: number
        flame:
          type: number
        water:
          type: number
        wood:
          type: number
        shine:
          type: number
        dark:
          type: number
        imageUrl:
          type: string
          nullable: true
        weapon:
          $ref: '#/components/schemas/WeaponResponse'
          nullable: true
        item:
          $ref: '#/components/schemas/ItemResponse'
          nullable: true

    UseItemRequest:
      type: object
      required: [itemId]
      properties:
        itemId:
          type: integer

    ChangeWeaponRequest:
      type: object
      required: [weaponId]
      properties:
        weaponId:
          type: integer

    BossFinishResponse:
      type: object
      required: [clearTime, experiencePoint, level, hitPoint]
      properties:
        clearTime:
          type: integer
          description: クリアタイム（ミリ秒）。サーバーが battles.updated_at - battles.created_at で計算
        experiencePoint:
          type: integer
        level:
          type: integer
        hitPoint:
          type: integer

    GameStartRequest:
      type: object
      required: [count]
      properties:
        count:
          type: integer
          description: 作成する新規ユーザー数
          minimum: 1

    BattleStartRequest:
      type: object
      required: [monsterToken]
      properties:
        monsterToken:
          type: string
          description: AES暗号化されたモンスタートークン

    BattleStartResponse:
      type: object
      required: [token, seed]
      properties:
        token:
          type: string
          format: uuid
        seed:
          type: integer

    BattleAction:
      type: object
      required: [turn, type]
      properties:
        turn:
          type: integer
        type:
          type: string
          enum: [attack, use-item, change-weapon]
        itemId:
          type: integer
          description: use-item のとき必須
        weaponId:
          type: integer
          description: change-weapon のとき必須

    BattleFinishRequest:
      type: object
      required: [token, actions]
      properties:
        token:
          type: string
          format: uuid
        actions:
          type: array
          items:
            $ref: '#/components/schemas/BattleAction'

    BattleRewardResponse:
      type: object
      required: [experiencePoint, level, hitPoint]
      properties:
        experiencePoint:
          type: integer
        level:
          type: integer
        hitPoint:
          type: integer
        drop:
          nullable: true
          allOf:
            - type: object
              required: [type]
              properties:
                type:
                  type: string
                  enum: [weapon, item]
                weaponId:
                  type: integer
                  nullable: true
                itemId:
                  type: integer
                  nullable: true

    UserStoreRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string

    UserStoreResponse:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid

