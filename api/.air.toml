[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "gen", "internal/infrastructure/db/sqlc"]
  delay = 1000

[log]
  time = true
